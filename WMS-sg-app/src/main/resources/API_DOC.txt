Documenting certain API's that do not have UI's
1. /auto-gen-approvals in AccApprGenController class - Auto generates ACC TRAN RECORDS,TASK MASTER AND TASKMASTER DTL based
	 on the location and approver ID.
	 
2. endpoints in the ErpController class - for Integration with OpenBravo used for confirmation 
	of dispatches and for tran/stock reconciliation
	
3. endpoints in GoApptiveController class - GoApptive integration used for updating fieldstaff data

4. /call_dsp_appr_manual - For dispatch final approval failure but BPM success

5. /push-dispatch-to-194R -  for pushing article delivery dispatches to 194 R
spring scheduler present for Dispatch push to 194 R (runs every hour)- Refer below 
@Scheduled(fixedRate = 3600000) 
@PostMapping("/push-dispatch-to-194R")

6. /ksybfelsjcywmkghpsef - for changing encrypted password

7. spring scheduler present for HeartBeat auto Logout service (runs every 3 hours)- HeartbeatService

8./specialAllocationApproval-manual 
speacial allocation send for approval Manual